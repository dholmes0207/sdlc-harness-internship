# Auto-Decision Ledger — 260719-1435-product-search-api

> Quyết-định con-AI TỰ ra ở chế-độ tự-quyết (KHÔNG phải sổ DEC user-duyệt `docs/decisions.md`). Sổ **chỉ để đọc, advisory** — không chặn việc gì. Nguồn sự-thật = `artifacts/auto-decisions.jsonl`; file này là VIEW sinh ra.

## ⚠ Phải soát (chưa)

| id | label | in_plan | skill/mode | what | why | evidence | reviewed |
| --- | --- | --- | --- | --- | --- | --- | --- |
| cd9066850f03 | SCOPE | no | hs:cook/auto | Hardened two phase-5 API tests post-phase by adding response-body assertions: test_api_page_size_100_ok now asserts total==20 and len(items)==20; test_api_unknown_query_param_ignored now asserts total==20 and len(items)==10. | The independent tester flagged both as asserting status code only on a 200 response -- the exact silent-200 pattern the plan spent six phases eliminating, and which tests/api/test_routes.py's own docstring warns against. test_api_unknown_query_param_ignored was the worse one: its name promises the param is IGNORED, but the assertion only proved it was not REJECTED; a bogus param that fell into a filter and zeroed results to total 0 would still have passed. Main scanned and found 10 status-only API tests total, but the other 8 are legitimate -- for 422/404 cases the status IS the contract. User was asked and chose to harden now. Discrimination proven by sabotage. Noted honestly that test_api_page_size_100_ok cannot separate that specific bug because at page_size=100 all 20 items fit one page so total equals len(items) coincidentally. | tests/api/test_routes.py:223 and :245 \| sabotage total=len(items) -> assert 10 == 20 FAILED \| app/main.py restored byte-exact, suite 131 passed | no |
| e0bdfb60d89a | SCOPE | no | hs:cook/auto | Rewrote phase-1 test_app_package_importable from a bare 'import app' into a subprocess guard that spawns .venv/bin/pytest against a probe file in a tempdir outside the tests/ package chain, so it actually fails when pythonpath=['.'] is removed. | The phase file claimed this test locks 'pythonpath cau hinh dung' and predicted 3 RED failures. Both claims are false: CPython's -m inserts cwd at sys.path[0], AND pytest's prepend import mode inserts the repo root via the tests/__init__.py package chain. Proven: the original test passed with config nulled out (-c /dev/null). It was a third instance of the RK20 'guard gia' pattern the plan already fixed twice (A10, A12). User was asked and approved building a real guard rather than accepting or deleting it. | tests/unit/test_smoke.py:30-92 \| phase-1-setup.md:160-161 claims pythonpath guard \| 3-state toggle: present=1 passed, removed=1 failed ModuleNotFoundError: No module named 'app', restored=4 passed | no |
| f4eec5b45d49 | SCOPE | no | hs:cook/auto | Phase 6 appends API documentation BELOW the user's existing submission content in README.md and refreshes the stale phase-progress list, instead of extending the header the phase file describes. | README.md changed between sessions and no longer matches the plan. Phase 6 Requirement #7 mandates preserving '# SDLC Harness Internship' + a description line (105 bytes, OBSERVED at plan time). The file is now 839 bytes of user-authored submission material (release links, THAY_BANG_LINK_RELEASE placeholder, progress list) and the content to be preserved no longer exists. The progress list also reads 'Cook Phase 1, Cook Phase 2' while phases 3-5 are complete. User was asked and chose to append below and refresh the status list, leaving the release-link placeholder untouched as theirs to fill. The curl table must stay in README.md because test_readme_curl_examples_match_reality parses that exact path. | README.md 839 bytes at commit 11923f1 'docs: add submission links' \| phase-6-docker-docs.md:39-41 asserts 105 bytes \| README.md progress list names only Cook Phase 1 and Phase 2 | no |


## Đã soát

_(none)_


## Chỉ truy-vết

| id | label | in_plan | skill/mode | what | why | evidence | reviewed |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 6cabbfc67601 | ASSUMPTION | no | hs:cook/auto | Accepted two phase-5 OBSERVED failure-mode symptoms that do NOT reproduce against the real implementation, without changing code or weakening the guards. | RK2: the plan documents reversed routes yielding '200 {hit:by_id,product_id:search}'. Reproduced side by side: that is what a STUB by-id handler produces; the REAL handler (which 404s on unknown id) turns the same routing bug into a 404. The routing bug is identical, only the symptom differs -- so the plan's headline rationale for R3 ('status-only assert PASSES while broken') does NOT hold for this implementation. R3 is kept anyway: it is mandatory per docs/code-standards.md:58-60, body assertion is strictly stronger and free, and the silent-200 returns the moment by-id stops 404ing. RK3: bare TestClient raises AttributeError rather than serving a silent 200 with total:0, because state has no fallback -- fail-loud, which is what docs/code-standards.md:78-79 actually wants. Both guards still fail correctly against the broken code. | scratchpad/verify_p5_traps.py: REAL handler -> 404 {'detail':'not found: search'}; STUB handler -> 200 {'hit':'by_id','product_id':'search'}; bare TestClient -> AttributeError 'State' object has no attribute 'dataset'; with-CM -> 200 products_loaded=20 | no |
| 79bfefaf9b58 | ASSUMPTION | no | hs:cook/auto | Phase-5 route-order verification uses a decorator-PREFIX grep (@app.get("/products/search") without the closing paren) instead of the phase's literal full-decorator grep. | The phase file contradicts itself: Requirements #9 mandates response_model= on the SAME decorator line, which makes the Success section's exact-string grep '@app.get("/products/search")' return NO MATCH (exit 1) against a spec-compliant main.py. Verified both greps directly. The prefix form preserves the original intent (VL-24: be immune to the mandatory warning comments, which never contain the literal '@app.get(' substring) and correctly reports 123 < 146. | app/main.py:123 @app.get("/products/search", response_model=PaginationEnvelope) \| app/main.py:146 @app.get("/products/{product_id}", response_model=ProductOut) \| literal grep exit 1, prefix grep 123<146 | no |

